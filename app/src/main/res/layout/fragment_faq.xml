<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.faq.FaqFragment">

    <!-- ScrollView to contain the entire content -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <!-- Linear layout to hold the content vertically -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- This LinearLayout will hold dynamically added card views for submitted questions -->
            <LinearLayout
                android:id="@+id/container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="16dp">
                <!-- Dynamically added card views will be placed here -->
            </LinearLayout>

            <!-- Divider line between input section and submitted questions -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/black"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp" />

        </LinearLayout>
    </ScrollView>

    <!-- CardView for each submitted question -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp">

        <!-- TextView to display the current user's name -->
        <TextView
            android:id="@+id/usernameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Current User Name"
            android:textStyle="bold"
            android:layout_below="@id/FaqTextE"
            android:layout_alignParentStart="true"
            android:layout_marginTop="8dp"/>

        <!-- EditText for user input -->
        <EditText
            android:id="@+id/FaqTextE"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your text" />

        <!-- Button to submit the question -->
        <Button
            android:id="@+id/button_submit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_below="@id/FaqTextE"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="8dp"/>


    </RelativeLayout>

</RelativeLayout>


